# 提高净收益专项报告（进行中）

> 目标：影子盘策略净收益 ≥ 13%。三条渠道：ML 双轨重训（含融资融券特征）、
> GitHub 公式动物园扩充、AlphaFeed 接口接入。
> 更新：2026-08-31（未填数字见下轮）。

## 一、ML 双轨重训（目标净收益 ≥ 13% 的核心）

特征集：34 基线 + 316 动物园公式 + 8 融资融券（拥挤度 × 双向）≈ 345 特征。
协议不变：Purged-KFold/embargo + walk-forward（train 2010–2019 / val 2020–2021 /
test 2022–2025），test 一次性 OOS。

| 实验 | 特征 | test rank_ic | test ICIR | test LS 净年化 | 备注 |
|---|---|---|---|---|---|
| GPU MLP v1 | 108 | **0.0558** | **8.28** | **+0.81%** | 对决 v2 通过 promote 门（Sharpe 2.35 vs 在任池 1.63，2M 资金） |
| LightGBM v1 扩特征 | 108 | 0.0493 | 5.47 | -0.50% | |
| GPU MLP v2（本轮） | 345 | 0.0529 | 7.57 | +0.06% | 特征扩充=负结果：额外 237 动物园+8 margin 特征未提升，反而稀释（LS Sharpe 2.17→1.69） |
| **XGBoost-GPU v1（本轮）** | 345 | **0.0566** | 6.28 | **+1.03%** | 345 特征集上的最佳模型级结果（GBDT 选择性 > MLP 抗噪）；GPU 训练（500 树，purged 早停 115） |
| LightGBM v2（本轮） | 345 | 待补 | 待补 | 待补 | 12 线程训练，运行中 |

**13% 目标口径说明**：对决 v2 已证明 MLP 簿在 2M 资金、真实成本下 test 窗
净年化 +19.5%（≥13% ✅）——但该数字是单窗 OOS；本轮重训的验证目标是把
特征面扩到 345 并保持/提升该水平，同时用组合级门检验短周期候选是否值得
放开 min_lookback 纪律。

## 二、GitHub 渠道增量

* GTJA 第二来源（Daic115/alpha191.py）补齐 88 条缺失公式 → 191/191；
* 翻译器四方方言（WorldQuant/GTJA/AQML/qlib/Daic115），动物园 237 → **316 条**
  全部可求值；新增算子 ts_sma/ts_count/ts_vwap/ts_resi；
* amount/VWAP 解锁：成交额数据本就在库（12.53M 条 100% 覆盖），市场面板恢复
  该列 + `VWAP→Div(Amount, Volume)` 映射；
* 32 条短周期可靠候选的组合级可交易性门：脚本就绪，待跑（生产 eval+risk 门，
  涨跌停掩码 + 危机窗口）。

## 三、AlphaFeed 接入

全端点实测可用（密钥在 .env）：快照 5,551 行含 amount、日K/分钟K/日内走势
（OHLCV+amount）、盘口 5 档。适配器扩展（60/min 限速）+ 3 测试。核心收益：
**确认成交额数据源**，为 VWAP/成交额类特征与盘中数据（分钟/盘口）铺路。

## 四、待办与判定点

1. LightGBM v2 结果 → 对决 v3（每类工件保留最近 2 个，v1/v2 同台对比）→ promote 门
   + 13% 净收益判定；
2. 32 候选组合级门 → min_lookback 60 纪律是否放开的证据；
3. **下一轮特征工程方向（v2 教训）**：LightGBM 特征重要性过滤（保留 top-K 如
   64–128 个）+ 再训 MLP，替代"全部 345 特征直接上"；融资融券特征单独消融
   （有/无两组对比）以确认其边际价值为负或零；
4. 若 v1 MLP 仍最优：影子盘接线（新工件类型 → 离线打分包装 → autopilot 替换门）。
